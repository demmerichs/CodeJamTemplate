#!/usr/bin/env python3

import numpy as np

ri = np.random.randint  # produces random numbers from [l, r)
rf = np.random.rand
perm = np.random.permutation

print(100000)
print(" ".join(map(str, list(range(1, 100001)))))
